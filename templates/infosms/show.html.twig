{% extends 'base.html.twig' %}

{% block title %}Infosms{% endblock %}

{% block body %}
    <h1>Infosms</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ infosm.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ infosm.libelle }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ infosm.contenu }}</td>
            </tr>
            <tr>
                <th>Dateajout</th>
                <td>{{ infosm.dateajout ? infosm.dateajout|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Publier</th>
                <td>{{ infosm.publier ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('infosms_index') }}">back to list</a>

    <a href="{{ path('infosms_edit', {'id': infosm.id}) }}">edit</a>

    {{ include('infosms/_delete_form.html.twig') }}
{% endblock %}
